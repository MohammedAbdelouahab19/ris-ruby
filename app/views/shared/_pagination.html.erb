<div id="pagination" data-turbo-prefetch="false" class="sticky left-0 right-0 mt-2 md:mt-0 flex flex-col md:flex-row justify-between py-2 md:py-4 space-y-5 md:space-y-0 overflow-auto">
  <div class="text-sm">
    <%= page_entries_info records %>
  </div>
  <%= paginate records, paginated_page_url: local_assigns[:paginated_page_url] ? paginated_page_url : nil %>
</div>
